import type { ClientPlugin } from '../interface';

/**
 * Amora Living Memory Hub — client plugin.
 * Currently uses standard Sera behavior with no customizations.
 * Add hooks here as Amora-specific requirements emerge.
 */
const plugin: ClientPlugin = {};

export default plugin;
