[build]
builder = "nixpacks"

[deploy]
startCommand = "node -e \"const t=process.env.SERVICE_TYPE;require(t==='dashboard'?'./dist/dashboard/server':t==='api'?'./dist/api/server':'./dist/worker')\""
restartPolicyType = "on_failure"
restartPolicyMaxRetries = 5
